%h3= t('views.documents.document.title')
- if documents.empty?
  %p= t('views.documents.document.empty')
- else
  %ul
    - documents.each do |doc|
      %li
        -# = link_to basename(doc), doc
        = link_to doc do
          - if doc.evidence.present? && doc.evidence.image?
            = image_tag(doc.evidence.thumb.url)
          = basename(doc)
        %small
          (
          = link_to(t('views.documents.show.edit'), edit_document_path(doc))
          |
          = link_to t('views.documents.show.delete'), document_path(doc), data: { confirm: "Are you sure?" }, method: :delete
          )
