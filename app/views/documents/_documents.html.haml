%h3= t('views.documents.document.title')
- if documents.empty?
  %p= t('views.documents.document.empty')
- else
  %ul
    - documents.each do |doc|
      %li
        -# = link_to basename(doc), doc
        = link_to doc do
          - if doc.evidence.present? && doc.evidence.image?
            = image_tag(doc.evidence.thumb.url)
          = basename(doc)
