%h3 Your claims
%table.table
  %thead
    %tr
      %th Date created
      %th Lost wages (est.)
      %th Actions
  %tbody
    - @claims.each do |claim|
      %tr
        %td= claim.created_at.strftime("%d %B, %Y")
        %td= number_to_currency(claim.lost_wages)
        %td
          = link_to "", claim_path(claim), class: "btn btn-default glyphicon glyphicon-eye-open", title: "View claim"
          = link_to "", edit_claim_path(claim), class: "btn btn-default glyphicon glyphicon-pencil", title: "Edit claim"
        
