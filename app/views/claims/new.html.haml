%section#introduction
  -# :markdown
  -#   #{ @skin.intro }
  %h3{ role: "presentation" }
    = t('.title')
  :markdown
    #{ @skin.intro.blank? ? t('.intro') : @skin.intro }

= render 'form', locals: { remote: true }
