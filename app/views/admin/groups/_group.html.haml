%tr
  %td= group.name
  %td= link_to group.slug, admin_groups_path(skin: group)
  %td= link_to '', admin_group_path(group), class: "btn btn-default btn-sm glyphicon glyphicon-eye-open", title: "View"
  %td= link_to '', edit_admin_group_path(group), class: "btn btn-default btn-sm glyphicon glyphicon-pencil", title: "Edit"
  %td= link_to '', admin_group_path(group), method: :delete, data: { :confirm => 'Are you sure?' }, class: "btn btn-default btn-sm glyphicon glyphicon-trash", title: "Delete"
